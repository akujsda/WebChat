type Message {
  senderId: String!
  recipientId: String!
  text: String!
}

input NewMessage {
  recipientId: String!
  senderId: String!
  text: String!
}


type Mutation {
  sendMessage(input: NewMessage!): Message!
}

type Query {
  getMessage(senderId: String!): [Message!]!
}
